---
title: Git & GitHub
description: Basic Git Commands
---

## গিট কি ?

গিট হলো একটি `ভার্সন কনট্রোল` টুল।

ভার্সন কনট্রোল কি ?

ধরুন আপনি আপনার কম্পিউটার এ কোন ডকুমেন্ট লিখছেন,সেখানে হয়তো আজ কিছু লিখছেন আবার হয়তো অন্যদিন কিছু লিখছেন। এভাবে হয়তো আপনার কখনও মনে হলো যে আপনি গত দুইদিন যা লিখেছেন তা হয়তো সঠিক নয়, আপনি তা ডিলিট করে আবার দুই দিন আগে যা লিখেছেন সেই অবস্থায় ফিরত যেতে চান। এক্ষেত্র আপনাকে যেটা করা লাগবে তা হলো এই দুইদিনের সমস্ত লিখা মুছে ফেলতে হবে। এখন এই অবস্থায় আপনার যদি মনে না থাকে যে আপানর ঠিক কোন পর্যন্ত মুছা উচিত বা দুইদিন আগে ঠিক কতটুকু লেখা হয়েছিলো,তখন আপনি কি করবেন? ঠিক এই কাজটায় গিট হেল্প করে থাকে। গিট কোন ফাইলের প্রতিটা চেঞ্জেস গুলোকে মনিটরিং করে বা ট্র্যাক রাখে এবং প্রতিটা চেঞ্জেস গুলোকে ভার্সনাইজ করে। যাতে আপনি যেকোন সময় যেকোনো ভার্সনে সুইচ করতে পারেন ।


###  1. **গিট ইনিশিয়ালাইজ করা**

```bash
 git init
```

ব্যাখ্যা:
প্রথমে আপনি যখন গিট প্রজেক্টে কাজ শুরু করবেন, আপনাকে প্রজেক্টটি গিট দিয়ে ইনিশিয়ালাইজ করতে হবে। এটি কমান্ড দিলে আপনার প্রজেক্ট ফোল্ডারে একটি .git ডিরেক্টরি তৈরি হবে, যা গিটের রেপোজিটরি হিসেবে কাজ করবে।



###  2. **গিট এর স্ট্যাটাস চেক করা**

```bash
git status
```

ব্যাখ্যা:
এটি আপনাকে জানাবে কোন ফাইলগুলি মডিফাই হয়েছে, কোন ফাইল নতুন যোগ হয়েছে, এবং কোন ফাইল এখনও কমিট করা হয়নি। এটি গিটের বর্তমান স্টেজ এবং রিপোজিটরির অবস্থা দেখার জন্য একটি দরকারী কমান্ড।


###  3. **ফাইলকে গিটের ট্রাক করার জন্য স্টেজ এ উঠানো**

```bash
git add <filename> //For stage any specific file
or
git add --all // if you want to add all files
or
git add .  // if you want to add all files in the current working directory
```
ব্যাখ্যা:
git add কমান্ডটি ব্যবহার করে আপনি ফাইল বা ফোল্ডারগুলোকে স্টেজে নিয়ে আসতে পারবেন, যা পরবর্তীতে কমিটের জন্য প্রস্তুত করা হয়।



###  4. **কমিট করা**

```bash
git commit -m "<commit massage>"
```

ব্যাখ্যা:
এই কমান্ডটি দিয়ে আপনার করা পরিবর্তনগুলো গিটে স্থায়ীভাবে সংরক্ষণ করা হয়। -m এর মাধ্যমে আপনি একটি ছোট্ট বার্তা দিতে পারবেন, যা সেই কমিটের বিবরণ দেয়।




###  5. **গিট লগ চেক করা**

```bash
git log
or
git log --oneline
```

ব্যাখ্যা:
এই কমান্ডটি দিয়ে আপনি পূর্বের সব কমিট দেখতে পারবেন। --oneline ফ্ল্যাগ ব্যবহার করলে প্রতিটি কমিটের সংক্ষিপ্ত তথ্য এক লাইনে দেখা যায়।





###  6. **একটা কমিট থেকে অন্য কমিটের অবস্থায় ফিরে যাওয়া**

```bash
git reset --hard <commit id>
```

ব্যাখ্যা:
এই কমান্ড দিয়ে আপনি নির্দিষ্ট একটি কমিটে ফিরে যেতে পারবেন এবং বর্তমান কাজের সব পরিবর্তন মুছে ফেলবেন।



###  7. **গিটের প্রতিটা চেঞ্জের রেফারেন্স সহ লগ দেখার জন্য**

```bash
git reflog
```

ব্যাখ্যা:
git reflog দিয়ে আপনি প্রতিটি চেঞ্জের রেফারেন্স দেখতে পারবেন। এটি প্রতিটি কমান্ডের ফলাফল দেখায়, যেমন কোন ব্রাঞ্চে কাজ হয়েছে, এবং কখন সুইচ করা হয়েছে।




###  8. **গিটের ট্র্যাক থেকে কোন ফাইল রিমূভ করা**

```bash
git rm <filename>
```

ব্যাখ্যা:
git rm দিয়ে আপনি গিটের ট্র্যাক থেকে কোনো ফাইল সরিয়ে দিতে পারবেন। এটি ফাইলকে রেপোজিটরি থেকে পুরোপুরি মুছে ফেলে।



###  9. **ব্রাঞ্চ তৈরী করা**

```bash
git branch <branch name eg: bugfix/fixname >
```

ব্যাখ্যা:
এই কমান্ডটি দিয়ে আপনি একটি নতুন ব্রাঞ্চ তৈরি করতে পারবেন, যেখানে আপনি স্বাধীনভাবে কাজ করতে পারবেন। উদাহরণস্বরূপ, bugfix/fixname নামে একটি ব্রাঞ্চ তৈরি করা যেতে পারে।



###  10. **ব্রাঞ্চ লিস্ট দেখা**

```bash
git branch --list
```

ব্যাখ্যা:
git branch --list দিয়ে আপনি গিটের সমস্ত ব্রাঞ্চের তালিকা দেখতে পারবেন।




###  11. **এক ব্রাঞ্চ থেকে অন্য ব্রাঞ্চ এ সুইচ করা**

```bash
git switch <branch name >
```

ব্যাখ্যা:
এই কমান্ডটি ব্যবহার করে আপনি একটি ব্রাঞ্চ থেকে অন্য ব্রাঞ্চে পরিবর্তন করতে পারবেন। এটি আগের git checkout এর বিকল্প।

 <Callout type="info">
    💡 এক্ষেত্রে যেই ব্রাঞ্চে অন্য ব্রাঞ্চের কোড আনতে হবে,প্রথমে `git switch <branchname>` করে প্রথমে সেই ব্রাঞ্চে যেতে হবে। তারপর কমান্ড দিতে হবে।
</Callout>

```bash
git branch -m <updated branch name >
```

15. **গিট স্ট্যাস করা (আনকমিটেড কাজ গিটের ড্রাফট এ সেভ রাখা)**

```bash
git stash
```

16. **গিট স্ট্যাস লিস্ট চেক করা**

```bash
git stash list
```

17. **গিটের স্ট্যাস এ কি কি সেভ রাখা হয়েছিল তা দেখতে**

```bash
git stash show -p
```

18. **গিটের স্ট্যাস এপ্লাই করার জন্য**

```bash
git stash apply <statsh id>
or
git stash pop
```

19. কোন ফাইল ভুলবশত গিটে এড করে ফেললে বা কমিট করার পর যদি মনে হয় ফাইলটা গিট থেকে ইগনোর করতে হবে তাহলে:



### ১ম স্টেপঃ

একটা .gitignore ফাইল বানাতে হবে এবং তাতে উক্ত ফাইলের নাম এড করা লাগবে ।

### ২য় স্টেপঃ

```bash
git rm --cached <filename>
```



20. গিটহাব বা রিমোট রিপোজিটরিতে ফাইল এড করা বা পুশ করার জন্যঃ


### ১ম স্টেপঃ

```bash
git remote add <origin name eg: orgin or something > <github repository link>
```

example:

`git remote add origin git@github.com:Deveripon/reactive-accelarator.git`

### ২য় স্টেপঃ

```bash
git push -u <origin name> <branch name>
```

example:

`git push -u origin main`



21. রিমোট রিপোজিটরি থেকে পুল করা

```bash
git pull
```

